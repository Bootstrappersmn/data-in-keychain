import XCTest
@testable import data_in_keychain

final class data_in_keychainTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(data_in_keychain().text, "Hello, World!")
    }
}
